import { BaseFilter } from './Base';
import type { Filter } from '../filter';
/**
 * Represents a "string search" where one or more string
 * properties are searched for a string value.
 *
 * Score is 0 or 1 depending on if the string exists
 */
export interface QueryDefinition extends Filter.BaseDefinition {
    kind: 'query';
    value: string | null;
    minLength?: number;
}
export declare class QueryFilter extends BaseFilter<QueryDefinition, string> {
    validate(): boolean;
    resolve(): Filter.Resolved;
}
