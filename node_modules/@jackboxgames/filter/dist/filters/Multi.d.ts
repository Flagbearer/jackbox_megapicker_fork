import { BaseFilter } from './Base';
import type { Filter } from '../filter';
/**
 * Represents a filter that compares an array of string or number
 * values against another array of values.
 *
 * Score is between 0-1 depending on intersection of the two arrays.
 */
export interface MultiDefinition extends Filter.BaseDefinition {
    kind: 'multi';
    value: (number | string)[] | null;
    propertyKey: string;
}
export declare class MultiFilter extends BaseFilter<MultiDefinition, (string | number)[]> {
    private validateArray;
    validate(): boolean;
    resolve(): Filter.Resolved;
}
