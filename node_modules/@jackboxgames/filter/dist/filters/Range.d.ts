import { BaseFilter } from './Base';
import type { Filter } from '../filter';
/**
 * Represents a filter checking if value is between two
 * test numbers inclusive.
 *
 * Score is 0 or 1 depending on if the property is between test values.
 */
export interface RangeDefinition extends Filter.BaseDefinition {
    kind: 'range';
    value: [number, number] | null;
    propertyKey: string;
}
export declare class RangeFilter extends BaseFilter<RangeDefinition, number> {
    validate(): boolean;
    resolve(): Filter.Resolved;
}
