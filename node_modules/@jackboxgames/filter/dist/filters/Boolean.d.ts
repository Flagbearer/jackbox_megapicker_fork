import { BaseFilter } from './Base';
import type { Filter } from '../filter';
/**
 * Represents a simple filter than can be either true or false.
 *
 * Score is 0 or 1 depending on if value matches property.
 */
export interface BooleanDefinition extends Filter.BaseDefinition {
    kind: 'boolean';
    value: boolean | null;
    propertyKey: string;
}
export declare class BooleanFilter extends BaseFilter<BooleanDefinition, boolean> {
    validate(): boolean;
    resolve(): Filter.Resolved;
}
