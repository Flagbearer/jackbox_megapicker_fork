import { BaseFilter } from './Base';
import type { Filter } from '../filter';
/**
 * Represents a filter that either:
 * - compares two string|number values.
 * - checks if a (string|number)[] array contains a test value.
 *
 * Score is 0 or 1 depending on if the value matches property.
 */
export interface ValueDefinition extends Filter.BaseDefinition {
    kind: 'value';
    value: number | string | null;
    propertyKey: string;
}
export declare class ValueFilter extends BaseFilter<ValueDefinition, number | string> {
    validate(): boolean;
    resolve(): Filter.Resolved;
}
