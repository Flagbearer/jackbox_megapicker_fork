import type { Filter } from '../filter';
export declare function get(key: string, item: Record<string, any>): any;
export declare abstract class BaseFilter<TDef extends Filter.BaseDefinition, TProp> {
    definition: TDef;
    protected properties: TProp[];
    constructor(definition: TDef, item: Filter.Item);
    abstract validate(): boolean;
    abstract resolve(): Filter.Resolved;
}
