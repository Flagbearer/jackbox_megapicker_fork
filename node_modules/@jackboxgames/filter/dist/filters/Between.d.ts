import { BaseFilter } from './Base';
import type { Filter } from '../filter';
/**
 * Represents a filter checking if a test value is between two
 * number properties inclusive.
 *
 * Score is 0 or 1 depending on if the test value is between properties.
 */
export interface BetweenDefinition extends Filter.BaseDefinition {
    kind: 'between';
    value: number | null;
    propertyKey: [string, string];
}
export declare class BetweenFilter extends BaseFilter<BetweenDefinition, number> {
    validate(): boolean;
    resolve(): Filter.Resolved;
}
